---
title: Button
description: Displays a button or a component that looks like a button.
---

import ButtonDemo from "~/registry/examples/button-example";

<Tabs defaultValue="code">
  <TabsList >
    <TabsTrigger value="code">code</TabsTrigger>
    <TabsTrigger value="preview">preview</TabsTrigger>
  </TabsList>
  <TabsContent value="code">

    <ComponentPreview>
      <ButtonDemo />
    </ComponentPreview>

  </TabsContent>
  <TabsContent value="preview">

```file="~/registry/examples/button-example" frame=none showLineNumbers

```

  </TabsContent>
</Tabs>

## Installation

### CLI

```package-exec
solidui-cli@latest add button
```

### Manual

Install the following dependencies:

```package-install
@kobalte/core
```

Copy and paste the following code into your project.

```file="~/registry/ui/button.tsx" title="~/components/ui/button.tsx" showLineNumbers

```

## Usage

```tsx
import { Button } from "~/components/ui/button";
```

```tsx
<Button variant="outline">Button</Button>
```

## Cursor

Tailwind v4 [switched](https://tailwindcss.com/docs/upgrade-guide#buttons-use-the-default-cursor) from `cursor: pointer` to `cursor: default` for the button component.

If you want to keep the `cursor: pointer` behavior, add the following code to your CSS file:

```css showLineNumbers title="globals.css"
@layer base {
  button:not(:disabled),
  [role="button"]:not(:disabled) {
    cursor: pointer;
  }
}
```
